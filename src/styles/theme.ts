// import * as colors from "./colors";

// type ThemeColors = typeof colors.NEUTRAL &
//   typeof colors.PRIMARY &
//   typeof colors.STROKE &
//   typeof colors.BACKGROUND &
//   typeof colors.SYSTEM;

// Define the type for the entire theme
type Theme = {
  // colors: ThemeColors;
};

const theme: Theme = {
  // colors: {
  //   ...colors.NEUTRAL,
  //   ...colors.PRIMARY,
  //   ...colors.STROKE,
  //   ...colors.BACKGROUND,
  //   ...colors.SYSTEM,
  // },
};
export default theme;
